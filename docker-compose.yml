services:
  shell:
    build: .
    image: eeweather
    stdin_open: true
    tty: true
    entrypoint: /bin/sh
    volumes:
      - .:/app

  test:
    image: eeweather
    entrypoint: py.test -n0
    volumes:
      - .:/app
      - /app/tests/__pycache__/

  uv:
    image: eeweather
    entrypoint: uv
    volumes:
      - .:/app
